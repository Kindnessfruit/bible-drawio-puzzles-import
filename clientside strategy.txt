'/api'



'/'

upon loading, 
create i_ according to url; if (!url){create first i_}

use i_, create and append (populate) form elements 
create initial l_ for select menus

on 'more' || 'less', 
  append new element into i_, 
  create and append (populate) form elements

on value change, 
  update i_,
  fetch '/api' as post request, 
  then update l_, form options

on "submit", 
  fetch '/api?i_=[...]&r=1' and populate the textarea
